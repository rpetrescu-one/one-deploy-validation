---
# Storage benchmark for one (cluster, template) pair. Included from main.yml
# with loop_var _sb_pair = [cluster_entry, template_name].
#
# Harness shape: block (test) / rescue (record_failure) / always (terminate
# the benchmark VM by its deterministic name). Cleanup never depends on
# variables registered inside the failed block.

- name: Resolve benchmark cluster, template and naming
  ansible.builtin.include_tasks: naming.yml

# Non-default templates (extra entries in validation.storage_benchmark.templates)
# must pre-exist, cluster-suffixed, exactly as the previous lookup expected.
# rc != 0 (broken CLI/frontend) is NOT treated as "missing": the harness block
# below then fails into rescue with the real error instead of recording a
# bogus skip.
- name: "Check that non-default template '{{ _sb_template }}{{ current_cluster.name_suffix }}' exists"
  ansible.builtin.shell: >
    onetemplate list --csv -f NAME='{{ _sb_template }}{{ current_cluster.name_suffix }}' -l ID --no-header
  register: _sb_tpl_precheck
  failed_when: false
  changed_when: false
  when: not (_sb_default_template | bool)

- name: Flag a missing non-default template
  ansible.builtin.set_fact:
    _sb_tpl_missing: >-
      {{ (not (_sb_default_template | bool))
         and ((_sb_tpl_precheck.rc | d(1)) == 0)
         and (((_sb_tpl_precheck.stdout | d('')) | trim) == '') }}

- name: "Record skip — template not found"
  ansible.builtin.include_role:
    name: validation_common
    tasks_from: record_result.yml
  vars:
    result_key: "{{ _sb_report_key }}"
    result_value: "skipped — template not found"
  when: _sb_tpl_missing | bool

# The when: at the bottom of this block is inherited by rescue and always too:
# a skipped pair created nothing, so nothing runs.
- name: Run Storage benchmark test
  block:
  - name: "Debug: induced failure hook"
    ansible.builtin.fail:
      msg: "Induced failure (validation.debug.fail_in=storage_benchmark)"
    when: (odv_fail_in | d((validation.debug | d({})).fail_in | d(''))) == 'storage_benchmark'

  # Default template: reuse the image test_vm exported for this cluster when
  # available; otherwise (validation.run_test_vm=false, or test_vm skipped
  # this cluster) export/reuse it ourselves. ensure_test_template.yml is
  # idempotent and registers template+image in the run manifest.
  - name: "Ensure default test template exists in cluster '{{ current_cluster.name }}'"
    ansible.builtin.include_role:
      name: validation_common
      tasks_from: ensure_test_template.yml
    vars:
      ensure_app_name: "{{ _sb_default_template_name }}"
      ensure_name: "{{ _sb_default_template_name }}{{ current_cluster.name_suffix }}"
      ensure_ds_id: "{{ current_cluster.image_ds_id }}"
      ensure_role: storage-benchmark
    when:
      - _sb_default_template | bool
      - current_cluster.name not in _test_vm_template_image_ids_map

  - name: Get non-default VM template as JSON
    ansible.builtin.shell: >
      onetemplate show "{{ _sb_template }}{{ current_cluster.name_suffix }}" -j
    register: image_output
    failed_when: "image_output.rc != 0 and image_output.stdout == ''"
    when: not (_sb_default_template | bool)

  - name: Set benchmark image ID for this cluster
    ansible.builtin.set_fact:
      _sb_image_id: >-
        {{ _test_vm_template_image_ids_map[current_cluster.name]
           if ((_sb_default_template | bool)
               and current_cluster.name in _test_vm_template_image_ids_map)
           else (_export_image_id
                 if (_sb_default_template | bool)
                 else (image_output.stdout | from_json | json_query('VMTEMPLATE.TEMPLATE.DISK.IMAGE_ID'))) }}

  - name: Assert the benchmark disk image was resolved
    ansible.builtin.assert:
      that: _sb_image_id | string | length > 0
      fail_msg: >-
        Could not resolve a disk image for template
        '{{ _sb_template }}{{ current_cluster.name_suffix }}'
        in cluster '{{ current_cluster.name }}'.

  - name: "Manifest: register benchmark VM '{{ _sb_vm_name }}'"
    ansible.builtin.include_role:
      name: validation_common
      tasks_from: manifest_add.yml
    vars:
      manifest_type: vm
      manifest_id: ""
      manifest_name: "{{ _sb_vm_name }}"
      manifest_cluster: "{{ current_cluster.name | d('') }}"
      manifest_role: storage-benchmark
      manifest_disposition: created

  # retries kept from the original task: if a failed attempt still created
  # the VM, the retry fails on the already-taken name and the always:
  # cleanup below removes the leftover by name.
  - name: Instantiate benchmark VM
    ansible.builtin.shell: >
      onetemplate instantiate "{{ _sb_template }}{{ current_cluster.name_suffix }}"
      --name '{{ _sb_vm_name }}'
      --nic '{{ current_cluster.storage_vnet_name }}' --disk {{ _sb_image_id }}:size=8000 --mem 1g
    register: benchmarkvm_id
    failed_when: "benchmarkvm_id.rc != 0"
    retries: 3
    delay: 20

  - name: Wait for benchmark VM to come up
    ansible.builtin.shell: >
      onevm list --f ID={{ benchmarkvm_id.stdout.split(':')[1] | trim }} -l STAT --no-header
    register: benchmarkvm_state
    failed_when: "benchmarkvm_state.rc != 0"
    until: benchmarkvm_state.stdout == "runn"
    retries: 10
    delay: 20

  - name: Get Benchmark VM IP
    ansible.builtin.shell: >
      onevm list --f ID={{ benchmarkvm_id.stdout.split(':')[1] | trim }} -l IP --no-header
    register: vm_ip
    failed_when: "vm_ip.rc != 0"

  - name: Verify ssh connection to the benchmark VM
    ansible.builtin.shell: >
      ssh -i ~oneadmin/.ssh/id_rsa -q -oStrictHostKeyChecking=no root@{{ vm_ip.stdout }}
    register: ssh_conn_result
    until: ssh_conn_result.rc == 0
    retries: 20
    delay: 20

  - name: Set DNS on benchmark VM
    become: true
    become_user: oneadmin
    ansible.builtin.shell: >
      ssh -q -oStrictHostKeyChecking=no root@{{ vm_ip.stdout }}
      'printf "nameserver 1.1.1.1\nnameserver 8.8.8.8\n" > /etc/resolv.conf'
    when: not (validation.storage_benchmark.skip_fio_install | d(false))

  # Semi air-gapped deployments use an image with fio preinstalled.
  - name: Install fio on benchmark VM
    become: true
    become_user: oneadmin
    ansible.builtin.shell: |
      ssh -q -oStrictHostKeyChecking=no root@{{ vm_ip.stdout }} apk update > /dev/null
      ssh -q -oStrictHostKeyChecking=no root@{{ vm_ip.stdout }} apk add fio > /dev/null
    when: not (validation.storage_benchmark.skip_fio_install | d(false))

  - name: Run Storage benchmark suite
    become: true
    become_user: oneadmin
    ansible.builtin.shell: |
      ssh -q -oStrictHostKeyChecking=no root@{{ vm_ip.stdout }} 'cat > /tmp/onebench.fio << "EOF"
      [global]
      time_based=1
      group_reporting=1
      direct=1
      ioengine=libaio
      filename=/tmp/onebench.testfile
      size=4G
      randrepeat=0
      norandommap=1
      invalidate=1
      thread=1
      lat_percentiles=1
      percentile_list=99
      ramp_time=5
      runtime=5

      [rr_4k_qd1]
      rw=randread
      bs=4k
      iodepth=1
      numjobs=1
      stonewall

      [rr_4k_qd4]
      rw=randread
      bs=4k
      iodepth=4
      numjobs=1
      stonewall

      [rr_4k_qd16]
      rw=randread
      bs=4k
      iodepth=16
      numjobs=1
      stonewall

      [rr_4k_qd64]
      rw=randread
      bs=4k
      iodepth=64
      numjobs=1
      stonewall

      [rr_4k_qd32]
      rw=randread
      bs=4k
      iodepth=32
      numjobs=1
      stonewall

      [rw_4k_qd1]
      rw=randwrite
      bs=4k
      iodepth=1
      numjobs=1
      stonewall

      [rw_4k_qd4]
      rw=randwrite
      bs=4k
      iodepth=4
      numjobs=1
      stonewall

      [rw_4k_qd16]
      rw=randwrite
      bs=4k
      iodepth=16
      numjobs=1
      stonewall

      [rw_4k_qd64]
      rw=randwrite
      bs=4k
      iodepth=64
      numjobs=1
      stonewall

      [rw_4k_qd32]
      rw=randwrite
      bs=4k
      iodepth=32
      numjobs=1
      stonewall

      [mix_4k_70r_qd16]
      rw=randrw
      bs=4k
      iodepth=16
      rwmixread=70
      numjobs=1
      stonewall

      [mix_64k_70r_qd16]
      rw=randrw
      bs=64k
      iodepth=16
      rwmixread=70
      numjobs=1
      stonewall

      [seqr_1m_qd4]
      rw=read
      bs=1m
      iodepth=4
      numjobs=1
      stonewall

      [seqr_128k_qd16]
      rw=read
      bs=128k
      iodepth=16
      numjobs=1
      stonewall

      [seqw_1m_qd4]
      rw=write
      bs=1m
      iodepth=4
      numjobs=1
      stonewall

      [seqw_128k_qd16]
      rw=write
      bs=128k
      iodepth=16
      numjobs=1
      stonewall

      [syncwrite_4k_qd1]
      rw=write
      bs=4k
      iodepth=1
      fsync_on_close=1
      end_fsync=1
      numjobs=1
      stonewall
      EOF'
      ssh -q -oStrictHostKeyChecking=no root@{{ vm_ip.stdout }} "fio /tmp/onebench.fio --output-format=json"
    register: fio_suite
    failed_when: "fio_suite.rc != 0"

  - name: Parse storage benchmark results
    ansible.builtin.set_fact:
      storage_benchmark: >-
        {{
          storage_benchmark | default({}) | combine({
            (inventory_hostname ~ ' (' ~ _sb_template ~ ')' ~ current_cluster.report_suffix): {
              'fio_raw': (fio_suite.stdout | from_json),
              'profiles': (fio_suite.stdout | from_json | json_query('jobs[].{name: jobname, rw: "job options".rw, bs: "job options".bs, iodepth: "job options".iodepth, read_iops: read.iops, read_bw_kib: read.bw, read_p99_ns: read.clat_ns.percentile."99.000000", write_iops: write.iops, write_bw_kib: write.bw, write_p99_ns: write.clat_ns.percentile."99.000000" }') )
            }
          }, recursive=True)
        }}

  - name: "Record storage benchmark success for cluster '{{ current_cluster.name }}'"
    ansible.builtin.include_role:
      name: validation_common
      tasks_from: record_result.yml
    vars:
      result_key: "{{ _sb_report_key }}"
      result_value: "ok"

  when: not (_sb_tpl_missing | bool)
  rescue:
    - name: Record storage benchmark failure
      ansible.builtin.include_role:
        name: validation_common
        tasks_from: record_failure.yml
      vars:
        failure_key: "{{ _sb_report_key }}"

    - name: Show failure
      ansible.builtin.debug:
        msg: >-
          Storage benchmark ({{ _sb_template }}) failed in cluster
          '{{ current_cluster.name }}', continuing with the next pair.

  always:
    # Role-owned cleanup: state-independent (finds the VM by its deterministic
    # name, never via variables registered in the block), idempotent, and
    # never aborts the run (failed_when: false throughout).
    - name: "Cleanup: terminate benchmark VM(s) named '{{ _sb_vm_name }}'"
      ansible.builtin.include_role:
        name: validation_common
        tasks_from: terminate_vm_by_name.yml
      vars:
        tvbn_name: "{{ _sb_vm_name }}"
        tvbn_role: storage-benchmark
        tvbn_retries: 30
